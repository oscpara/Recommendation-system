# Every i th epoch 
   # Freeze embeddings 
     # for each user 
      # generate user embedding 
      # generate all positive item embeddings 
      # generate all negative item embeddings  + sample 
      #compute scores 
      # rank scores 
      # compute precision & recall @10